Lumen 2.1.0 / validation record / 7 September 2026

Executed locally:
- 93 Node regression tests passed (0 failures).
- 24 Chromium DOM checks passed (0 uncaught JavaScript errors).
- 3 ICC fixtures parsed and transformed successfully using LittleCMS.
- Syntax, HTML references/IDs and versioned relative imports checked.

DOM tests use mocked camera acquisition/readings, in-memory Peer transport and
storage doubles. Node tests cover all four deterministic plans; the controller
DOM run completes Quick, with first-sample failure, clipping, movement,
reconnection, pause/resume and export checks. No normal browser navigation,
physical iPhone Safari, actual WebRTC/NAT, monitor accuracy or OS GPU calibration
loading was validated here. This is not evidence of a real-device successful run.

Reproduce with instructions in TESTING.md. Real-device acceptance is required.
